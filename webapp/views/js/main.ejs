

// variables needed





// DOM-id's and such

var formMonster = document.getElementById('insertMonster');



// database

function insertMonster(monsterName, mosnterCharct, monsterLocation, monsterFood, monsterDesc) {
	r.table('user').insert({

		name: monsterName,
		charcteristics: monsterCharct,
		location: monsterLocation,
		food: monsterFood,
		description: monsterDesc

	}).run(conn, function(err, result) {

		if(err) throw err;
		console.log(result);

	});
}





 // event listeners

//function getMonsterInfo() {

	formMonster.addEventListener('submit', function(event) {

		event.preventDefault();

		var monsterName 		= getElementById('monsterName').value,
			monsterCharct 		= getElementById('monsterCharct').value,
			monsterLocation 	= getElementById('monsterLocation').value,
			monsterFood 		= getElementById('monsterFood').value,
			monsterDesc 		= getElementById('monsterDesc').value;

			console.log(monsterName);
			console.log(monsterCharct);
			console.log(monsterLocation);
			console.log(monsterFood);
			console.log(monsterDesc);

	});

//}
